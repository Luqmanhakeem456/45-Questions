let members: string[] = ["Asif", "Rahim", "Nasir", "Karim"];
let message: string = "Who have complete assignment?;";
for(var i=0; i < members.length; i++ ) {
    console.log(message + members[i]);
}